apiVersion: v1
kind: Service
metadata:
  name: {{ release_name }}
  namespace: {{ mysql_namespace }}
spec:
  selector:
    app: {{ release_name }}
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
